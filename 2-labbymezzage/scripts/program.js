var messageBoard = {
    // The array should store all the messages
    messages: [],
    init: function () {
        alert("Kanppen fungerar!");
    }
};

// document.getElementById("send-button").addEventListener("click", messageBoard.init());